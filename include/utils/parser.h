/*
 * parse_file
 * A partir de un archivo de tipo string
 * lo parsea y devuelve un stack/pila con todas 
 * las instrucciones en su orden logico
 * ya cargadas.
 *     
 * */
mstack_t parse_file(char *);
